import React from "react";

interface IProps {
  [key: string]: any;
}

const Auth: React.FC<IProps> = () => {
  return (
    <div className="Auth">
      <div>Auth</div>
    </div>
  );
};

export default Auth;
