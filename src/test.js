let braces = " ((";

// let count = 0;
// validParentheses(braces); // true or false
//
// function validParentheses (braces) {
 // console.log(braces);
 // let cleanedString = braces.replace(/[^\(\)]/g);
  //if(cleanedString.length<2 || !cleanedString.length%2) return false;

  //let leftBraces = cleanedString.match(/\(/g);
  //let rightBraces = cleanedString.match(/\)/g);

  //if(!leftBraces || !rightBraces || leftBraces.length !== rightBraces.length) return false;
//
//   let counter = 0;
//   for(let i=0; i<cleanedString.length; i++){
//     cleanedString[i] === "(" && counter++;
//     cleanedString[i] === ")" && counter--;
//     if(counter < 0) break;
//   }
//   return counter < 0 ? false : true;
// }
// const obj: any = {
//   user: {
//     first: 'nick'
//   }
// };
// console.log(obj['user']);
//
// let a = '"user.name.first":"Nick","user.name.second":"NO nick"'
// console.log(a.split('.').map(item => {
//   return item
// }));

//console.log(validParentheses(braces));
